{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ErrorConfig",
  "description": "Configuration for error handling and display within UI components, specifying error message formats, retry behavior, fallback content, and user guidance. Ensures consistent error UX.",
  "allOf": [
    {
      "$ref": "../../base/spec-node.schema.json"
    }
  ],
  "properties": {
    "spec_node_id": {
      "const": "ux.errorconfig"
    },
    "layer_id": {
      "const": "ux"
    },
    "type": {
      "const": "errorconfig"
    },
    "attributes": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "name": {
          "type": "string"
        },
        "showNotification": {
          "type": "boolean"
        },
        "notificationType": {
          "type": "string",
          "enum": ["success", "error", "warning", "info", "neutral"],
          "description": "Severity category of the notification, aligned with ARIA live region and alert role semantics."
        },
        "errorMessage": {
          "type": "string"
        },
        "defaultErrorMessage": {
          "type": "string"
        },
        "retryable": {
          "type": "boolean",
          "description": "Whether the failed operation can be retried by the user."
        },
        "maxRetries": {
          "type": "integer",
          "description": "Maximum number of retry attempts before the error is considered permanent."
        }
      },
      "additionalProperties": false
    }
  }
}
