{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Reference",
  "description": "A JSON Schema $ref pointer that references another schema by URI or JSON Pointer fragment. Enables schema reuse and composition without duplication.",
  "allOf": [
    {
      "$ref": "../../base/spec-node.schema.json"
    }
  ],
  "properties": {
    "spec_node_id": {
      "const": "data-model.reference"
    },
    "layer_id": {
      "const": "data-model"
    },
    "type": {
      "const": "reference"
    },
    "attributes": {
      "type": "object",
      "properties": {
        "$ref": {
          "type": "string",
          "description": "URI or JSON Pointer fragment referencing another schema"
        }
      },
      "additionalProperties": false
    }
  }
}
