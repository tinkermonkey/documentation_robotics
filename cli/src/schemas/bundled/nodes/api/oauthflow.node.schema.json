{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OAuthFlow",
  "description": "Configuration for a single OAuth 2.0 grant type (implicit, password, clientCredentials, or authorizationCode). authorizationUrl is required for implicit and authorizationCode flows; tokenUrl is required for password, clientCredentials, and authorizationCode flows; scopes is required for all flows.",
  "allOf": [
    {
      "$ref": "../../base/spec-node.schema.json"
    }
  ],
  "properties": {
    "spec_node_id": {
      "const": "api.oauthflow"
    },
    "layer_id": {
      "const": "api"
    },
    "type": {
      "const": "oauthflow"
    },
    "attributes": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "name": {
          "type": "string"
        },
        "authorizationUrl": {
          "type": "string"
        },
        "tokenUrl": {
          "type": "string"
        },
        "refreshUrl": {
          "type": "string"
        },
        "scopes": {
          "type": "object",
          "description": "Map of scope name to human-readable description string"
        }
      },
      "required": ["scopes"],
      "additionalProperties": false
    }
  }
}
