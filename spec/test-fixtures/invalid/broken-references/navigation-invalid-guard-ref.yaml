# Invalid fixture - Route references non-existent guard
# Should fail: Navigation reference validation
# Error: Route references guard that doesn't exist

version: "0.1.0"
application: "test-app"

routes:
  - identifier: "protected-route"
    name: "Protected Route"
    type: experience
    guards: ["non-existent-guard"]  # Guard doesn't exist
    addressing:
      url: "/protected"
