# Invalid fixture - References non-existent elements
# Should fail: Cross-layer reference validation
# Error: References to non-existent elements

- id: application.service.invalid-refs
  type: service
  name: Service with Invalid References
  description: This service references elements that don't exist
  properties:
    realizes: business.service.non-existent  # Does not exist
    deployedTo: technology.node.missing-node  # Does not exist
    uses:
      - application.service.another-missing  # Does not exist
    securedBy:
      - security.policy.fake-policy  # Does not exist
