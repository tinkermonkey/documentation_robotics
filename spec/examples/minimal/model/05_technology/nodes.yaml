# Minimal Example - Technology Layer
# Single deployment node

- id: technology.node.app-server
  type: node
  name: Application Server
  description: Server hosting the application
  properties:
    type: container
    runtime: docker
    hosts:
      - application.service.app-service
