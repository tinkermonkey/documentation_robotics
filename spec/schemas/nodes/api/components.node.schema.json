{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Components",
  "description": "A named registry for reusable API definition objects (schemas, responses, parameters, examples, requestBodies, headers, securitySchemes, links, callbacks) that enables $ref referencing throughout the specification. Component entries are not rendered directly — they must be referenced by operations, parameters, or schemas to take effect.",
  "allOf": [
    {
      "$ref": "../../base/spec-node.schema.json"
    }
  ],
  "properties": {
    "spec_node_id": {
      "const": "api.components"
    },
    "layer_id": {
      "const": "api"
    },
    "type": {
      "const": "components"
    },
    "attributes": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "format": "uuid"
        },
        "name": {
          "type": "string"
        },
        "schemas": {
          "type": "array",
          "description": "Contains relationship"
        },
        "responses": {
          "type": "array",
          "description": "Contains relationship"
        },
        "parameters": {
          "type": "array",
          "description": "Contains relationship"
        },
        "examples": {
          "type": "array",
          "description": "Contains relationship"
        },
        "requestBodies": {
          "type": "array",
          "description": "Contains relationship"
        },
        "headers": {
          "type": "array",
          "description": "Contains relationship"
        },
        "securitySchemes": {
          "type": "array",
          "description": "Contains relationship"
        },
        "links": {
          "type": "array",
          "description": "Contains relationship"
        },
        "callbacks": {
          "type": "array",
          "description": "Contains relationship"
        }
      },
      "additionalProperties": false
    }
  }
}
